const cheeringWorkTitle = "今日も頑張りましょう！"
const goodWorkTitle = "一日お疲れ様でした！"
const currentTimeTitle = "現在時刻："
const totalWorkingHourTitle = "合計"
const diffWorkingHourTitle = (min, max) => {
    return `差分(min${min}h, max${max}h)`
}